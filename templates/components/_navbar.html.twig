<nav class="navbar navbar-expand-lg bg-body-tertiary">
    <div class="container-fluid">
        <a class="navbar-brand" href="{{ path('app_default_home') }}">OnlyInAmerica</a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNavAltMarkup" aria-controls="navbarNavAltMarkup" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNavAltMarkup">
            <div class="navbar-nav ms-auto">
                <a class="nav-link active" aria-current="page" href="{{ path('app_product_productlist')}}">Produits</a>
                {% if app.user %}
                    {% if is_granted('ROLE_ADMIN')  %}
                        <a class="nav-link text-danger" href="{{ path('admin') }}">Administration</a>
                        <a class="nav-link " href="{{ path('app_user_userprofile') }}">Mon Profil</a>
                    {% elseif is_granted('ROLE_USER') %}
                        <a class="nav-link " href="{{ path('app_user_userprofile') }}">Mon Profil</a>
                    {% endif%}
                    <a class="nav-link" href="{{ path('app_logout') }}">Se déconnecter</a>
                {% else %}
                    <a class="nav-link" href="{{ path('app_user_userregister') }}">S'inscire</a>
                    <a class="nav-link" href="{{ path('app_login') }}">S'authentifier</a>
                {% endif %}
                <a class="nav-link" href="{{ path('app_default_contact') }}">Contact</a>
                <a class="nav-link" href="{{ path('app_default_apropos') }}">A propos</a>
            </div>
        </div>
    </div>
</nav>